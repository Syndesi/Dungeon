import {assert} from 'chai';
/*
import Game from '../client/game.js';
import Keyboard from '../client/util/keyboard.js';

describe('Keyboard', function(){
  it('should initialize correctly', function(){
    var el = document.createElement('div');
    document.body.appendChild(el);
    var game = new Game(el, false);
    var key = new Keyboard(game.store);
    assert.instanceOf(key.keyCode, Object);
    assert.instanceOf(key.isKeyDown, Array);
    assert.instanceOf(key.keyDown, Array);
    assert.instanceOf(key.keyPress, Array);
    assert.instanceOf(key.keyUp, Array);
  });
});
*/